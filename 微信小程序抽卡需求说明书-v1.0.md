# 微信小程序抽卡需求说明书

## 1. 文档信息

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 编制日期 | 2026-09-16 |
| 产品阶段 | 两周体验版 MVP |
| 目标平台 | 微信小程序 |
| 开发方式 | IDEA 编码，微信开发者工具调试与发布 |
| 技术路线 | 原生微信小程序 + 微信云开发 |

## 2. 产品概述

### 2.1 产品定位

本产品是一款面向朋友和熟人的私人纪念卡抽取小程序。管理员使用本人或已获得明确肖像授权的人物照片制作卡牌，普通用户通过每日签到获得抽卡次数，抽取并收集卡牌。

首版不包含小游戏。后续小游戏可作为新的抽卡次数来源接入，但不得改变首版的账号、次数和收藏体系。

### 2.2 产品目标

目标按优先级排列：

1. 学习并实践原生微信小程序、JavaScript、云函数、云数据库和云存储的完整开发流程。
2. 让朋友以较低的使用门槛体验签到、抽卡和收集纪念卡的乐趣。
3. 为后续接入小游戏预留统一的奖励发放能力。

### 2.3 交付目标

- 在两周内完成可邀请朋友使用的微信小程序体验版。
- 首轮测试规模不超过 100 名用户、200 张卡牌。
- 正式提交审核和公开上线不属于本阶段验收条件。

## 3. 用户与权限

### 3.1 普通用户

普通用户可以：

- 静默登录小程序。
- 每日手动签到。
- 查看剩余抽卡次数。
- 进行单抽或十连抽。
- 查看本次抽卡结果。
- 查看自己的卡册与收集进度。

普通用户不能：

- 上传或管理卡牌图片。
- 查看其他用户的数据。
- 查看抽取概率、保底进度或历史抽卡记录。
- 访问管理员页面或调用管理员接口。

### 3.2 管理员

首版只设一名管理员，通过服务端维护的微信 `OpenID` 白名单识别。管理员在普通用户功能之外，可以：

- 上传卡牌图片。
- 创建和编辑卡牌。
- 发起图片内容安全检测。
- 确认肖像授权和人工审核结果。
- 配置稀有度显示名称、概率和保底次数。
- 配置常驻卡、限定卡、限定时间和抽取权重。
- 上架、普通下架或强制移除卡牌。
- 查看管理员操作日志。

管理员入口只在鉴权成功后显示。所有管理员云函数必须再次校验调用者身份，不能只依靠前端隐藏入口。

## 4. 产品范围

### 4.1 首版范围

- 微信静默登录。
- 每日签到并累计抽卡次数。
- 单抽和十连抽。
- 固定四档稀有度。
- 可配置的稀有度名称、概率和最高稀有度保底。
- 常驻卡与限定卡共用一个卡池。
- 同稀有度内按卡牌权重抽取。
- 重复卡数量累计。
- 全图鉴及灰色剪影展示。
- 管理员卡牌管理。
- 图片内容安全检测和人工授权确认。
- 卡牌普通下架、限定到期和强制移除。
- 抽卡流水、配置版本及管理员操作日志。

### 4.2 首版不包含

- 小游戏。
- 充值、支付、广告、积分商城或其他次数获取渠道。
- 连续签到、补签或漏签补偿。
- 排行榜、好友关系、评论、赠送、交换或交易。
- 普通用户上传图片。
- 用户昵称、头像、手机号或地理位置采集。
- 概率公示、保底进度展示和用户抽卡历史页面。
- 卡牌强化、组队、战斗或数值增益。
- 正式公开发布。

## 5. 信息架构

### 5.1 普通用户页面

普通用户采用两个主页面：

1. 抽卡首页
2. 卡册页

### 5.2 管理员页面

管理员页面包括：

- 卡牌列表
- 卡牌新增与编辑
- 图片检测与人工审核
- 稀有度与抽卡规则配置
- 上下架及强制移除
- 操作日志

## 6. 功能需求

### 6.1 静默登录

#### 功能说明

用户首次进入小程序时，系统通过云函数获取微信身份，并创建内部用户记录。后续进入时自动识别已有用户。

#### 规则

- 不要求用户授权昵称或头像。
- 用户记录以服务端获得的 `OpenID` 作为身份依据。
- 前端不得自行提交或修改 `OpenID`。
- 新用户初始抽卡次数为 0，保底计数为 0。
- 登录失败时不得进入签到、抽卡或卡册功能。

### 6.2 每日签到

#### 功能说明

用户在抽卡首页手动点击签到按钮。签到成功后获得 1 次抽卡机会。

#### 规则

- 每个自然日只能签到一次。
- 日期边界以服务端换算的北京时间 `00:00` 为准。
- 成功签到后，剩余抽卡次数增加 1。
- 抽卡次数可以跨天累计，不设上限和过期时间。
- 漏签不补发，不计算连续签到。
- 当日已签到时，按钮显示“今日已签到”并不可重复点击。
- 网络重试或快速重复点击不得重复发放次数。

#### 异常处理

- 签到失败时不改变签到状态和抽卡次数。
- 同一签到请求重复提交时，返回首次执行结果。

### 6.3 抽卡首页

抽卡首页同时承担签到和抽卡功能，包含：

- 今日签到状态。
- 剩余抽卡次数。
- 签到按钮。
- 当前卡池主题区域。
- 当前生效限定卡提示及结束时间。
- 单抽按钮。
- 十连按钮。
- 卡册入口。
- 仅管理员可见的管理入口。

次数不足时，对应抽卡按钮显示禁用状态，并提示用户次日签到获取次数。

### 6.4 卡池

#### 卡池结构

首版只有一个抽卡入口和一套全局保底计数。卡池由以下卡牌共同组成：

- 常驻卡：上架后持续参与抽取，直到管理员下架。
- 限定卡：仅在限定开始时间至结束时间内参与抽取。

一张卡牌只能属于一种类型，不能同时作为常驻卡和限定卡。同一张图片只能创建一张卡牌。

#### 卡牌公开信息

普通用户可见信息仅包括：

- 卡牌图片
- 卡牌名称
- 稀有度

#### 卡牌运营信息

管理员还需设置：

- 常驻或限定类型
- 同稀有度内的权重
- 限定开始和结束时间
- 上架状态
- 审核状态

### 6.5 稀有度与概率

#### 稀有度

- 系统固定四个内部稀有度档位，由低到高排列。
- 管理员可以修改四档的显示名称。
- 管理员不能新增、删除或调整档位顺序。

#### 概率配置

- 管理员为四档分别配置抽取概率。
- 概率以整数万分比保存，四档合计必须等于 `10000`。
- 系统不预设不可修改的业务概率；首次开放抽卡前，管理员必须发布一份有效配置。
- 概率配置发布后生成不可变的版本编号。
- 配置修改只影响发布后的新抽卡，不改变历史记录。

#### 卡牌权重

- 系统先抽取稀有度，再从该档当前有效卡牌中按权重抽取具体卡牌。
- 权重必须为正整数。
- 限定卡可以设置高于同档常驻卡的权重。
- 权重只在同一稀有度内部比较，不直接改变四档稀有度总概率。

### 6.6 最高稀有度保底

- 只有最高稀有度设置保底，其他档位不设保底。
- 管理员配置保底次数 `N`，`N` 必须为正整数。
- 用户连续 `N-1` 抽未获得最高稀有度时，第 `N` 抽必定获得最高稀有度。
- 用户通过普通概率提前抽中最高稀有度时，保底计数立即清零。
- 所有常驻卡和限定卡共用同一个保底计数。
- 十连按顺序执行十次独立抽取，每一抽都可能触发或重置保底。
- 修改保底次数不清除用户已有计数。
- 若用户已有计数达到或超过新的保底阈值，则下一抽直接触发保底。
- 首版不向普通用户显示保底次数或当前进度。

### 6.7 单抽和十连

#### 单抽

- 消耗 1 次抽卡机会。
- 成功后展示一张抽卡结果。

#### 十连

- 消耗 10 次抽卡机会。
- 不提供折扣或额外十连保底。
- 十次结果依次参与正常概率、权重和保底计算。
- 用户可以逐张揭示，也可以跳过动画查看汇总。
- 十连必须整体成功或整体失败，不允许只扣除部分次数或只生成部分结果。

#### 通用规则

- 每个抽卡请求携带唯一请求编号。
- 同一请求编号只能执行一次；重复提交时返回首次结果。
- 云函数成功完成数据写入后，前端才播放抽卡动画。
- 用户退出动画或关闭页面不会丢失已成功的抽卡结果。
- 首版不向普通用户展示概率、随机参数、历史记录或保底进度。

### 6.8 抽卡结果

每张结果展示：

- 卡牌图片
- 卡牌名称
- 稀有度
- 首次获得标识或重复获得标识
- 当前拥有数量

单抽采用“卡背出现、点击翻开、展示卡牌”的流程。十连可逐张揭示，并在最后展示汇总结果。最高稀有度使用更明显的卡框、光效和音效。

### 6.9 卡册

#### 展示规则

- 卡册展示所有当前有效卡牌。
- 已获得卡牌显示原图、名称、稀有度和拥有数量。
- 未获得卡牌显示灰色剪影、稀有度边框和 `???` 名称。
- 重复获得同一张卡时只增加拥有数量，不创建重复列表项。
- 顶部显示收集进度，例如“已收集 28/100”。
- 支持按全部和四档稀有度筛选。
- 点击已获得卡牌可查看大图。

#### 下架后的显示

- 普通下架卡和已结束限定卡：若用户已经获得，继续显示在该用户卡册中。
- 未获得的普通下架卡和已结束限定卡：不再作为当前有效图鉴目标展示。
- 强制移除卡：对所有用户隐藏，不计入收集总数。

### 6.10 管理员卡牌管理

#### 新增卡牌

管理员上传图片并填写：

- 卡牌名称
- 稀有度
- 卡牌类型
- 抽取权重
- 限定起止时间（限定卡必填）

#### 编辑规则

- 修改图片后，必须重新进行内容安全检测和人工授权确认。
- 只修改名称、稀有度、权重或限定时间时，无需重新检测图片，但必须重新通过发布校验。
- 已有抽卡流水不因后续编辑而改变。

### 6.11 内容审核与肖像授权

卡牌发布流程为：

`草稿 -> 安全检测中 -> 待人工确认 -> 可发布 -> 已上架`

发布必须同时满足：

1. 微信图片内容安全检测通过。
2. 管理员人工确认图片内容正确，并勾选“已取得肖像授权”。

任一条件未满足时，卡牌不得进入可抽取状态。系统保存检测状态、检测时间、人工确认状态和操作时间。

### 6.12 发布校验

卡牌或抽卡配置发布前必须校验：

- 卡牌名称不为空且长度符合界面限制。
- 图片存在并已通过内容安全检测。
- 管理员已确认肖像授权。
- 稀有度有效。
- 权重为正整数。
- 限定卡开始时间早于结束时间。
- 四档概率之和等于 100%。
- 每个概率大于零的稀有度至少存在一张当前可抽卡牌。
- 保底次数为正整数。

校验失败时禁止发布，并明确显示失败原因。

### 6.13 下架与强制移除

#### 普通下架

- 卡牌立即停止参与后续抽取。
- 已获得该卡的用户继续保留收藏。
- 不物理删除卡牌或历史抽卡记录。

#### 限定结束

- 到达结束时间后自动停止参与抽取。
- 已获得限定卡继续保留。
- 管理员可以在未来重新设置时间，使同一张限定卡再次参与抽取。

#### 强制移除

- 适用于肖像授权撤回、内容违规或其他必须删除展示的情况。
- 管理员必须填写移除原因并进行二次确认。
- 强制移除后，该卡对所有普通用户隐藏，并从收集总数中排除。
- 卡牌、收藏关系和历史流水保留为不可见审计数据，不执行物理删除。

## 7. 界面与视觉要求

### 7.1 整体风格

- 采用清新纪念册风格。
- 使用明亮、柔和的基础色和少量高对比强调色。
- 可使用相册、票根或拍立得式细节，但人物照片必须保持视觉主体地位。
- 不使用会遮挡或明显改变人物照片内容的复杂滤镜。
- 四档稀有度通过卡框颜色和细节区分。
- 适配常见手机尺寸和微信安全区域。

### 7.2 交互要求

- 签到和抽卡按钮具有明确的可用、禁用和处理中状态。
- 云函数处理中防止重复点击。
- 抽卡动画允许跳过，不阻塞结果写入。
- 首版只实现必要的翻卡、淡入和稀有度光效，不制作复杂 3D 动画。
- 图片加载失败时显示统一占位图，不改变卡牌数据。

## 8. 系统架构

### 8.1 技术组成

- 前端：原生微信小程序 WXML、WXSS、JavaScript。
- 后端：微信云函数。
- 数据：微信云数据库。
- 图片：微信云存储。
- 开发：IDEA。
- 预览、真机调试和上传：微信开发者工具。

不建设独立 Java 后端、独立服务器或关系型数据库。

### 8.2 职责边界

前端负责：

- 页面展示。
- 用户交互。
- 调用云函数。
- 展示云函数返回的状态和结果。

云函数负责：

- 获取可信用户身份。
- 管理员鉴权。
- 签到日期判断和次数发放。
- 抽卡次数校验和扣除。
- 概率、权重、保底和限定时间计算。
- 卡册更新。
- 抽卡流水和操作日志写入。
- 卡牌审核、发布、下架和强制移除。

### 8.3 抽卡数据流

一次抽卡按以下顺序执行：

`校验用户与请求编号 -> 检查次数 -> 查询有效卡牌 -> 读取概率配置 -> 判断保底 -> 抽取稀有度 -> 按权重抽取卡牌 -> 原子更新次数、保底和卡册 -> 写入流水 -> 返回结果`

关键数据更新必须具备事务性。十连要么全部成功，要么全部失败。

## 9. 数据设计

以下为概念数据结构，字段名可在实现阶段按统一命名规范调整，但语义不得改变。

### 9.1 `users`

- 用户内部编号
- 微信 `OpenID`
- 剩余抽卡次数
- 最近签到日期
- 最高稀有度未命中计数
- 创建时间
- 更新时间

### 9.2 `cards`

- 卡牌编号
- 卡牌名称
- 云存储图片标识
- 稀有度内部编号
- 卡牌类型：常驻或限定
- 权重
- 限定开始时间
- 限定结束时间
- 审核状态
- 上架状态
- 强制移除状态及原因
- 创建时间
- 更新时间

### 9.3 `collections`

- 用户编号
- 卡牌编号
- 拥有数量
- 首次获得时间
- 最近获得时间
- 可见状态

用户与卡牌的组合必须唯一。

### 9.4 `rarity_config`

- 配置版本号
- 四档内部编号
- 四档显示名称
- 四档概率万分比
- 最高稀有度保底次数
- 生效状态
- 发布时间

### 9.5 `draw_records`

- 请求编号
- 用户编号
- 单抽或十连类型
- 消耗次数
- 每一抽的顺序和卡牌结果
- 每一抽是否由保底触发
- 抽取前后保底计数
- 概率配置版本
- 抽取时间

### 9.6 `admin_logs`

- 管理员身份
- 操作类型
- 目标对象
- 修改前摘要
- 修改后摘要
- 操作原因
- 操作时间

## 10. 安全与隐私

- 云数据库关键集合不允许小程序端直接写入。
- 所有余额、概率、权重、保底和时间判断以服务端为准。
- 管理员 `OpenID`、云环境配置和密钥不得提交到公开仓库。
- 普通用户只能获取公开卡牌信息和本人的账户、收藏数据。
- 抽卡使用服务端不可预测随机数。
- 签到、抽卡和管理接口必须进行参数校验和频率限制。
- 首次使用时展示隐私说明，明确仅为身份识别和收藏功能处理必要数据。
- 正式公开上线前重新核对微信当期的隐私保护、内容安全和概率公示要求。

## 11. 异常处理

| 场景 | 处理要求 |
| --- | --- |
| 登录失败 | 阻止进入核心功能并提供重试 |
| 重复签到 | 不重复发放，返回今日已签到状态 |
| 抽卡次数不足 | 拒绝请求，不生成流水，不改变保底 |
| 重复请求编号 | 返回首次执行结果，不重复扣除或发卡 |
| 概率合计错误 | 暂停抽卡，管理员端显示配置错误 |
| 正概率稀有度无有效卡牌 | 暂停抽卡并向用户提示“卡池维护中” |
| 卡池无有效卡牌 | 暂停抽卡，不扣除次数 |
| 云函数执行失败 | 回滚本次关键数据修改 |
| 十连中发生错误 | 整体回滚，不保留部分结果 |
| 图片加载失败 | 显示占位图，保留卡牌信息 |
| 内容安全检测失败 | 保留草稿，禁止发布并显示原因 |
| 限定卡到期 | 自动退出抽取范围，已有收藏保留 |

## 12. 非功能要求

### 12.1 性能

- 目标规模：100 名用户、200 张卡牌。
- 普通页面在正常网络下目标加载时间不超过 2 秒。
- 抽卡云函数在正常情况下目标响应时间不超过 3 秒。
- 卡牌列表使用缩略图和分页或分批加载。
- 卡牌详情页按需加载大图。

### 12.2 可靠性

- 签到、抽卡和卡册更新必须保持数据一致性。
- 所有关键写操作使用唯一请求编号或等价幂等机制。
- 不允许出现次数已扣除但卡牌未到账的状态。
- 配置发布后生成版本快照，历史流水关联当时版本。

### 12.3 可观测性

- 云函数记录错误类型、请求编号和发生时间。
- 日志不得输出完整敏感身份信息。
- 抽卡流水在体验测试期间完整保留，不自动删除。
- 管理员关键操作必须可追溯。

## 13. 验收标准

### 13.1 登录与权限

- 新用户无需提供昵称和头像即可进入系统。
- 普通用户无法看到管理员入口。
- 普通用户伪造管理员参数时，云函数拒绝执行。
- 用户无法查询或修改其他用户的账户和收藏数据。

### 13.2 签到

- 同一北京时间自然日首次签到增加 1 次。
- 同日重复签到不增加次数。
- 跨日后可以再次签到。
- 快速重复点击和网络重试不重复发放。

### 13.3 抽卡

- 单抽准确扣除 1 次，十连准确扣除 10 次。
- 次数不足时不扣除、不发卡、不改变保底。
- 重复请求不会重复扣除或重复发卡。
- 十连失败时不产生部分结果。
- 四档概率、同档权重和最高稀有度保底按发布配置执行。
- 提前抽中最高稀有度后，保底计数清零。
- 保底触发后，结果必为当前有效的最高稀有度卡牌。

### 13.4 卡牌与卡册

- 常驻卡在上架期间可抽取。
- 限定卡只在开始时间至结束时间内可抽取。
- 重复卡正确累计数量。
- 未获得卡正确显示灰色剪影。
- 普通下架和限定结束后，已有收藏继续显示。
- 强制移除后，卡牌对所有普通用户隐藏且不计入收集总数。

### 13.5 管理端

- 未通过安全检测的图片不能发布。
- 未确认肖像授权的卡牌不能发布。
- 概率总和不为 100% 时不能发布配置。
- 正概率稀有度没有有效卡牌时不能开放抽卡。
- 限定时间无效时不能发布限定卡。
- 强制移除必须填写原因并记录日志。

### 13.6 容量与兼容性

- 在 100 名测试用户和 200 张卡牌范围内核心流程稳定运行。
- 在至少两种不同屏幕尺寸的微信真机环境中完成核心流程测试。

## 14. 两周开发安排

### 第一周：用户闭环

| 时间 | 工作内容 |
| --- | --- |
| 第 1–2 天 | 熟悉项目结构，配置云开发，完成静默登录 |
| 第 3 天 | 完成每日签到、次数累计和幂等处理 |
| 第 4–5 天 | 完成有效卡池、单抽、十连、权重和保底 |
| 第 6 天 | 完成卡册、重复数量和未获得剪影 |
| 第 7 天 | 整合抽卡首页并进行第一轮真机测试 |

### 第二周：管理与质量

| 时间 | 工作内容 |
| --- | --- |
| 第 8–9 天 | 完成管理员鉴权、卡牌上传和编辑 |
| 第 10 天 | 接入图片内容安全检测与人工授权确认 |
| 第 11 天 | 完成限定时间、权重、概率和保底配置 |
| 第 12 天 | 完成上下架、强制移除和操作日志 |
| 第 13 天 | 补充异常处理、隐私说明和多机型测试 |
| 第 14 天 | 修复问题、准备体验版并邀请朋友测试 |

## 15. 重点测试场景

- 北京时间 `23:59` 和次日 `00:00` 附近签到。
- 多次快速点击签到。
- 多次快速点击单抽或十连。
- 抽卡请求超时后由前端重试。
- 十连执行期间发生云函数异常。
- 保底前一抽、保底触发抽和提前抽中最高稀有度。
- 十连内部触发保底后，后续抽取从新计数开始。
- 限定卡开始和结束边界时刻。
- 管理员修改概率和保底后，历史流水仍关联旧版本。
- 某个正概率稀有度没有有效卡牌。
- 卡池完全为空。
- 图片内容检测失败或接口异常。
- 普通用户伪造管理员调用参数。
- 卡牌普通下架、限定到期和强制移除后的卡册表现。
- 图片加载失败时的占位图展示。

## 16. 后续扩展约束

后续小游戏接入时必须遵守以下边界：

- 小游戏通过统一的服务端奖励接口发放抽卡次数。
- 小游戏前端不能直接修改用户剩余次数。
- 每次奖励发放必须有唯一请求编号和来源记录。
- 小游戏与抽卡共享现有用户身份和次数余额。
- 首版卡册、卡池、抽卡流水和管理员能力无需因小游戏接入而重建。

## 17. 项目风险

- 开发者熟悉 Java，但需要同步学习 JavaScript、原生小程序和云开发，两周排期较紧。
- 微信内容安全接口的开通条件、调用权限或测试环境可能影响双重审核功能进度。
- 体验版可以完成开发验证，但正式上线仍受主体、类目、隐私和平台审核要求影响。
- 首版按需求不向用户展示概率；正式上线或未来加入付费、广告前，必须重新评估概率公示和合规要求。
- 人物照片即使通过内容安全检测，也不能替代肖像授权证明和人工判断。

## 18. 完成定义

满足以下条件时，首版视为达到体验版交付标准：

1. 本文第 13 章的核心验收项全部通过。
2. 关键异常场景不会导致次数、保底或卡册数据不一致。
3. 管理员可以完成一张卡从上传、检测、人工确认到上架的完整流程。
4. 普通用户可以完成登录、签到、单抽或十连、查看结果和查看卡册的完整流程。
5. 已通过微信开发者工具和至少两种真机尺寸验证。
6. 已生成可邀请朋友加入的微信小程序体验版。
