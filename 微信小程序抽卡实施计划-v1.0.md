# Cardory 微信小程序 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**目标：** 在两周内从空项目完成一个可邀请朋友测试的原生微信小程序体验版，实现静默登录、每日签到、单抽/十连、四档概率与保底、卡册和管理员卡牌管理。

**架构：** 小程序前端只负责界面和请求，所有身份、签到、抽卡、保底和管理权限由一个模块化云函数 `api` 处理。纯业务规则与微信 SDK 隔离并使用 Node.js 内置测试框架验证，数据库访问集中在仓储层，便于在本地使用内存仓储测试事务行为。

**技术栈：** 原生微信小程序（WXML/WXSS/JavaScript）、微信云开发、微信云函数、云数据库、云存储、CommonJS、Node.js 内置 `node:test` 与 `assert`。

**需求说明书：** `outputs/微信小程序抽卡需求说明书-v1.0.md`

## 全局约束

- 体验版目标规模为 100 名用户、200 张卡牌。
- 普通用户不提供上传功能，不收集昵称、头像、手机号或地理位置。
- 抽卡次数首版只通过每日签到获得，每日固定 1 次，可永久累计。
- 四档稀有度内部编号固定为 `N`、`R`、`SR`、`SSR`，仅显示名称可修改。
- 常驻卡和限定卡共用一个卡池、一种抽卡次数和一个最高稀有度保底计数。
- 所有关键写操作必须幂等；十连必须整体成功或整体失败。
- 普通用户不展示概率、保底进度或历史抽卡记录。
- 管理员权限只能由云函数根据 `OpenID` 白名单判断。
- 数据库集合禁止小程序端直接写入。
- 关键业务逻辑必须先写失败测试，再写最小实现。

---

## 1. 目标目录结构

在实现开始时创建独立项目目录 `cardory/`，不要使用当前工作区上层的 Git 仓库。

```text
cardory/
├─ miniprogram/
│  ├─ app.js
│  ├─ app.json
│  ├─ app.wxss
│  ├─ sitemap.json
│  ├─ components/
│  │  ├─ card-tile/
│  │  └─ draw-result/
│  ├─ pages/
│  │  ├─ home/
│  │  ├─ album/
│  │  ├─ card-detail/
│  │  └─ admin/
│  │     ├─ cards/
│  │     ├─ card-edit/
│  │     ├─ config/
│  │     └─ logs/
│  └─ utils/
│     ├─ api.js
│     ├─ request-id.js
│     └─ format.js
├─ cloudfunctions/
│  └─ api/
│     ├─ index.js
│     ├─ package.json
│     ├─ src/
│     │  ├─ router.js
│     │  ├─ errors.js
│     │  ├─ context.js
│     │  ├─ domain/
│     │  │  ├─ time.js
│     │  │  ├─ config.js
│     │  │  ├─ card-state.js
│     │  │  └─ draw-engine.js
│     │  ├─ services/
│     │  │  ├─ bootstrap-service.js
│     │  │  ├─ sign-in-service.js
│     │  │  ├─ draw-service.js
│     │  │  ├─ catalog-service.js
│     │  │  └─ admin-service.js
│     │  └─ repositories/
│     │     ├─ cloud-repository.js
│     │     └─ memory-repository.js
│     └─ test/
│        ├─ time.test.js
│        ├─ config.test.js
│        ├─ card-state.test.js
│        ├─ draw-engine.test.js
│        ├─ sign-in-service.test.js
│        ├─ draw-service.test.js
│        └─ admin-service.test.js
├─ docs/
│  ├─ database-schema.md
│  ├─ manual-test-checklist.md
│  └─ release-checklist.md
├─ project.config.json
└─ README.md
```

边界说明：

- `domain/` 只包含无网络、无数据库依赖的纯规则。
- `services/` 编排业务用例，只依赖仓储接口和领域函数。
- `repositories/` 负责微信云数据库或内存数据访问。
- `router.js` 只做动作分发、输入整形和统一错误返回。
- 页面不得直接访问云数据库。

---

### Task 1：建立项目骨架与自动测试入口

**文件：**

- 创建：`cardory/project.config.json`
- 创建：`cardory/miniprogram/app.js`
- 创建：`cardory/miniprogram/app.json`
- 创建：`cardory/miniprogram/app.wxss`
- 创建：`cardory/cloudfunctions/api/package.json`
- 创建：`cardory/cloudfunctions/api/index.js`
- 创建：`cardory/cloudfunctions/api/src/router.js`
- 创建：`cardory/cloudfunctions/api/src/errors.js`
- 创建：`cardory/cloudfunctions/api/test/router.test.js`
- 创建：`cardory/README.md`

**接口：**

- 输入：微信云函数事件 `{ action: string, payload?: object }`。
- 输出：统一结果 `{ ok: true, data }` 或 `{ ok: false, error: { code, message } }`。

- [ ] **步骤 1：在 `cardory/` 初始化独立 Git 仓库并创建微信项目骨架**

运行：

```powershell
New-Item -ItemType Directory -Path cardory
git -C cardory init
```

预期：`cardory/.git` 存在，`git -C cardory status --short --branch` 显示一个无提交的新仓库。

- [ ] **步骤 2：写统一路由的失败测试**

`test/router.test.js` 至少包含：

```js
const test = require('node:test');
const assert = require('node:assert/strict');
const { createRouter } = require('../src/router');

test('unknown action returns ACTION_NOT_FOUND', async () => {
  const route = createRouter({});
  const result = await route({ action: 'missing' }, {});
  assert.deepEqual(result, {
    ok: false,
    error: { code: 'ACTION_NOT_FOUND', message: '不支持的操作' }
  });
});
```

- [ ] **步骤 3：运行测试并确认失败**

运行：

```powershell
Set-Location cardory/cloudfunctions/api
npm test
```

预期：因 `src/router.js` 尚未提供 `createRouter` 而失败。

- [ ] **步骤 4：实现统一错误与路由**

`src/errors.js` 导出 `AppError`；`src/router.js` 导出 `createRouter(handlers)`。已知业务错误返回其代码和公开文案，未知异常记录日志并返回 `INTERNAL_ERROR`，不得向前端返回堆栈。

```js
class AppError extends Error {
  constructor(code, message) {
    super(message);
    this.code = code;
  }
}
module.exports = { AppError };
```

- [ ] **步骤 5：配置测试脚本并运行全部测试**

`package.json`：

```json
{
  "name": "cardory-api",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "engines": { "node": ">=18" },
  "scripts": { "test": "node --test" },
  "dependencies": { "wx-server-sdk": "3.0.1" }
}
```

运行：`npm install`，再运行：`npm test`。

预期：所有测试通过，退出码为 0。

- [ ] **步骤 6：在微信开发者工具导入项目并验证空白首页可启动**

使用测试 AppID 或项目 AppID，确认控制台无页面注册和云初始化错误。

- [ ] **步骤 7：提交骨架**

```powershell
git add .
git commit -m "chore: scaffold mini program and cloud API"
```

---

### Task 2：实现领域时间、配置与卡牌有效性规则

**文件：**

- 创建：`cloudfunctions/api/src/domain/time.js`
- 创建：`cloudfunctions/api/src/domain/config.js`
- 创建：`cloudfunctions/api/src/domain/card-state.js`
- 创建：`cloudfunctions/api/test/time.test.js`
- 创建：`cloudfunctions/api/test/config.test.js`
- 创建：`cloudfunctions/api/test/card-state.test.js`

**接口：**

- `beijingDateKey(date): string` 返回 `YYYY-MM-DD`。
- `validateRarityConfig(config): void`，非法时抛 `AppError`。
- `isCardDrawable(card, now): boolean`。

- [ ] **步骤 1：写北京时间边界测试**

```js
test('Beijing midnight changes date key', () => {
  assert.equal(beijingDateKey(new Date('2026-09-15T15:59:59Z')), '2026-09-15');
  assert.equal(beijingDateKey(new Date('2026-09-15T16:00:00Z')), '2026-09-16');
});
```

- [ ] **步骤 2：写配置校验和限定时间测试**

覆盖以下断言：

- 四档 ID 必须按 `N/R/SR/SSR` 出现且各出现一次。
- `probabilityBps` 必须是非负整数且合计 `10000`。
- `pityLimit` 必须是正整数。
- 常驻卡在 `PUBLISHED` 状态下一直有效。
- 限定卡在 `[startsAt, endsAt)` 内有效，恰好到结束时失效。
- `FORCE_REMOVED`、`DRAFT` 和 `OFFLINE` 状态不可抽取。

- [ ] **步骤 3：运行测试确认失败**

运行：`npm test`。

预期：三个领域模块不存在导致失败。

- [ ] **步骤 4：实现纯领域规则**

北京时间日期使用固定 UTC+8 计算，避免依赖手机时间：

```js
function beijingDateKey(date) {
  return new Date(date.getTime() + 8 * 60 * 60 * 1000)
    .toISOString()
    .slice(0, 10);
}
```

限定卡判断必须使用 `now >= startsAt && now < endsAt`，不能把结束时刻算入有效期。

- [ ] **步骤 5：运行全部测试并提交**

运行：`npm test`。

预期：全部通过。

```powershell
git add cloudfunctions/api/src/domain cloudfunctions/api/test
git commit -m "feat: add time and card configuration rules"
```

---

### Task 3：实现静默登录、用户初始化与管理员鉴权

**文件：**

- 创建：`cloudfunctions/api/src/context.js`
- 创建：`cloudfunctions/api/src/services/bootstrap-service.js`
- 创建：`cloudfunctions/api/src/repositories/memory-repository.js`
- 创建：`cloudfunctions/api/src/repositories/cloud-repository.js`
- 创建：`cloudfunctions/api/test/bootstrap-service.test.js`
- 修改：`cloudfunctions/api/index.js`
- 修改：`cloudfunctions/api/src/router.js`
- 创建：`docs/database-schema.md`

**接口：**

- `bootstrapService.execute({ openid }): Promise<UserSession>`。
- `UserSession` 包含 `userId`、`drawCredits`、`signedToday`、`isAdmin`。
- 仓储提供 `findUserByOpenid`、`createUser`、`isAdminOpenid`。

- [ ] **步骤 1：写新用户与已有用户测试**

```js
test('new user starts with zero credits and zero pity', async () => {
  const repo = createMemoryRepository();
  const service = createBootstrapService({ repo, now: () => new Date('2026-09-16T02:00:00Z') });
  const session = await service.execute({ openid: 'user-a' });
  assert.equal(session.drawCredits, 0);
  assert.equal(repo.snapshot().users[0].pityCount, 0);
});
```

另写测试确认同一 `OpenID` 重复启动不会创建第二个用户，以及 `admins` 集合存在匹配记录时返回 `isAdmin: true`。

- [ ] **步骤 2：运行测试确认失败**

运行：`npm test`。

预期：仓储和服务模块不存在导致失败。

- [ ] **步骤 3：实现仓储接口和初始化服务**

内部用户 ID 使用 `crypto.randomUUID()`。云仓储封装 `users`、`admins` 集合；`admins` 只允许通过云控制台预置，不提供普通 API 写入口。

- [ ] **步骤 4：接入微信可信上下文**

`index.js` 使用 `cloud.getWXContext().OPENID`，忽略前端 `payload` 中任何身份字段：

```js
exports.main = async (event) => {
  const { OPENID } = cloud.getWXContext();
  return route(event, { openid: OPENID });
};
```

- [ ] **步骤 5：写数据库结构文档**

明确 `users`、`admins`、`cards`、`collections`、`rarity_config`、`draw_records`、`operation_records`、`admin_logs` 八个集合的字段、索引和状态枚举。为 `users.openid`、`collections(userId, cardId)`、`draw_records._id` 和 `operation_records._id` 记录唯一性策略。`operation_records` 用于保存签到等非抽卡操作的幂等结果。

- [ ] **步骤 6：运行测试、云端部署并手动调用 `bootstrap`**

本地运行：`npm test`。

云端预期：首次调用创建一个用户，第二次调用返回相同 `userId`，数据库中只有一条用户记录。

- [ ] **步骤 7：提交**

```powershell
git add cloudfunctions/api docs/database-schema.md
git commit -m "feat: add identity bootstrap and admin authorization"
```

---

### Task 4：实现每日签到与幂等发放

**文件：**

- 创建：`cloudfunctions/api/src/services/sign-in-service.js`
- 创建：`cloudfunctions/api/test/sign-in-service.test.js`
- 修改：`cloudfunctions/api/src/repositories/memory-repository.js`
- 修改：`cloudfunctions/api/src/repositories/cloud-repository.js`
- 修改：`cloudfunctions/api/src/router.js`

**接口：**

- `signInService.execute({ openid, requestId }): Promise<{ signed: boolean, drawCredits: number, dateKey: string }>`。
- 仓储提供事务方法 `runUserTransaction(openid, callback)`。

- [ ] **步骤 1：写四个签到测试**

覆盖：首次签到加 1、同日第二次不增加、北京时间跨日后再加 1、相同请求编号重试返回第一次结果。

```js
assert.deepEqual(await service.execute({ openid: 'u1', requestId: 'r1' }), {
  signed: true,
  drawCredits: 1,
  dateKey: '2026-09-16'
});
```

- [ ] **步骤 2：运行测试确认失败**

运行：`npm test`。

预期：`sign-in-service.js` 不存在导致失败。

- [ ] **步骤 3：实现事务签到**

先以 `sha256(userId + ':signIn:' + requestId)` 查询 `operation_records`；存在时返回原始结果。不存在时，在同一事务内读取用户的 `lastSignInDate`：日期相同则保存“未重复发放”的幂等结果；日期不同则同时写入新日期、余额加 1 和成功结果。不得先读后在事务外更新。这样原请求在北京时间跨日后重试时也不会再次发放。

- [ ] **步骤 4：接入 `signIn` 路由并校验请求编号**

`requestId` 必须为 16–64 位字母、数字或连字符，非法值返回 `INVALID_REQUEST_ID`。

- [ ] **步骤 5：运行自动测试与并发手工测试**

运行：`npm test`。

在微信开发者工具同时触发两次签到请求，预期余额只增加 1。

- [ ] **步骤 6：提交**

```powershell
git add cloudfunctions/api
git commit -m "feat: add idempotent daily sign in"
```

---

### Task 5：实现可复现测试的抽卡领域引擎

**文件：**

- 创建：`cloudfunctions/api/src/domain/draw-engine.js`
- 创建：`cloudfunctions/api/test/draw-engine.test.js`

**接口：**

- `drawOne({ cards, config, pityCount, randomInt }): DrawOutcome`。
- `drawMany({ count, cards, config, pityCount, randomInt }): { outcomes, finalPityCount }`。
- `randomInt(maxExclusive)` 返回 `[0, maxExclusive)` 的整数；生产环境使用 `crypto.randomInt`，测试传入固定序列。

- [ ] **步骤 1：写稀有度概率边界测试**

使用配置 `N=6000, R=3000, SR=900, SSR=100`，固定随机值分别为 `0`、`5999`、`6000`、`8999`、`9000`、`9899`、`9900`、`9999`，断言每个边界落入正确档位。

- [ ] **步骤 2：写权重测试**

同档三张卡权重 `1/3/6`，固定随机值 `0/1/3/4/9`，断言选中区间分别符合累计权重。

- [ ] **步骤 3：写保底与十连顺序测试**

覆盖：

- `pityLimit=20` 且抽前计数为 19 时必出 `SSR`。
- 普通概率提前命中 `SSR` 后计数清零。
- 非最高稀有度结果使计数加 1。
- 十连第 3 抽触发保底时，第 4 抽从计数 0 继续。
- 只有当前有效卡牌参与同档权重选择。

- [ ] **步骤 4：运行测试确认失败**

运行：`npm test`。

预期：抽卡引擎不存在导致失败。

- [ ] **步骤 5：实现最小抽卡引擎**

实现顺序必须固定为：判断保底、抽稀有度、筛选对应稀有度卡牌、按权重抽卡、计算新保底计数。没有候选卡时抛 `POOL_MAINTENANCE`，不得自动降档。

生产随机函数：

```js
const { randomInt } = require('node:crypto');
```

- [ ] **步骤 6：运行全部测试并提交**

运行：`npm test`，预期全部通过。

```powershell
git add cloudfunctions/api/src/domain/draw-engine.js cloudfunctions/api/test/draw-engine.test.js
git commit -m "feat: add weighted draw and pity engine"
```

---

### Task 6：实现事务抽卡、收藏更新与抽卡流水

**文件：**

- 创建：`cloudfunctions/api/src/services/draw-service.js`
- 创建：`cloudfunctions/api/test/draw-service.test.js`
- 修改：`cloudfunctions/api/src/repositories/memory-repository.js`
- 修改：`cloudfunctions/api/src/repositories/cloud-repository.js`
- 修改：`cloudfunctions/api/src/router.js`

**接口：**

- `drawService.execute({ openid, requestId, count }): Promise<DrawResponse>`。
- `count` 只允许 `1` 或 `10`。
- `DrawResponse` 包含 `results`、`remainingCredits`，不包含概率和保底计数。

- [ ] **步骤 1：写服务层失败测试**

覆盖：余额不足、不支持的抽取数量、空卡池、错误配置、正概率档位无有效卡、相同请求编号重复提交。

- [ ] **步骤 2：写成功事务测试**

覆盖：

- 单抽扣 1，并创建一条收藏和一条流水。
- 重复卡只增加收藏数量。
- 十连扣 10，并按顺序保存十个结果。
- 十连中任一异常时余额、保底、收藏和流水全部保持原值。
- 返回结果包含 `isNew` 和 `ownedCount`。

- [ ] **步骤 3：运行测试确认失败**

运行：`npm test`。

预期：`draw-service.js` 不存在导致失败。

- [ ] **步骤 4：实现确定性幂等键**

抽卡流水 `_id` 使用 `sha256(userId + ':' + requestId)`。事务开始时先查该记录；存在时直接返回已保存响应，不再次扣除次数。

- [ ] **步骤 5：实现完整事务**

事务内完成：用户余额检查、有效卡读取、配置读取、顺序抽取、余额扣除、保底更新、收藏 upsert、流水写入。云仓储不得在事务完成前向前端返回结果。

- [ ] **步骤 6：运行测试并在云环境做一次单抽和十连**

本地：`npm test`。

云端检查数据库：余额变化、收藏数量、保底计数和流水结果一致。

- [ ] **步骤 7：提交**

```powershell
git add cloudfunctions/api
git commit -m "feat: add transactional single and ten draws"
```

---

### Task 7：实现图鉴查询与下架显示规则

**文件：**

- 创建：`cloudfunctions/api/src/services/catalog-service.js`
- 创建：`cloudfunctions/api/test/catalog-service.test.js`
- 修改：`cloudfunctions/api/src/repositories/memory-repository.js`
- 修改：`cloudfunctions/api/src/repositories/cloud-repository.js`
- 修改：`cloudfunctions/api/src/router.js`

**接口：**

- `catalogService.listAlbum({ openid, rarityId? }): Promise<AlbumResponse>`。
- 返回 `items`、`collectedUnique`、`totalCollectible`，不返回其他用户数据。

- [ ] **步骤 1：写卡册组合规则测试**

覆盖：有效未获得卡显示 `name: '???'` 且无图片 URL；有效已获得卡显示真实信息和数量；普通下架但已拥有的卡继续显示；普通下架且未拥有的卡不显示；强制移除卡无论是否拥有均不显示。

- [ ] **步骤 2：写稀有度筛选和计数测试**

断言筛选后列表仅包含指定内部档位，同时总收集进度按当前可收集卡和用户保留收藏计算。

- [ ] **步骤 3：运行测试确认失败**

运行：`npm test`。

- [ ] **步骤 4：实现查询服务和临时图片 URL 转换**

仓储只为允许显示原图的卡牌生成临时 URL。未获得卡不得把云文件标识返回给普通用户。

- [ ] **步骤 5：运行测试、云端验证并提交**

运行：`npm test`，预期全部通过。

```powershell
git add cloudfunctions/api
git commit -m "feat: add private album catalog"
```

---

### Task 8：实现小程序公共基础层与抽卡首页

**文件：**

- 创建：`miniprogram/utils/api.js`
- 创建：`miniprogram/utils/request-id.js`
- 创建：`miniprogram/utils/format.js`
- 创建：`miniprogram/pages/home/index.js`
- 创建：`miniprogram/pages/home/index.wxml`
- 创建：`miniprogram/pages/home/index.wxss`
- 创建：`miniprogram/pages/home/index.json`
- 修改：`miniprogram/app.js`
- 修改：`miniprogram/app.json`
- 修改：`miniprogram/app.wxss`

**接口：**

- `callApi(action, payload): Promise<data>` 统一调用云函数并转换业务错误。
- `createRequestId(): string` 生成 16–64 位请求编号。

- [ ] **步骤 1：实现 API 封装和请求编号**

`api.js` 只调用一个名为 `api` 的云函数，若返回 `ok: false`，抛出包含 `code` 的错误。`request-id.js` 使用时间戳加随机字节生成请求编号，不复用页面加载时的旧编号。

- [ ] **步骤 2：实现应用启动与静默登录**

`app.js` 初始化 `wx.cloud` 后调用 `bootstrap`，保存最小会话状态：`userId`、`drawCredits`、`signedToday`、`isAdmin`。不请求用户资料权限。

- [ ] **步骤 3：实现签到区域**

包含未签到、处理中、已签到、失败重试四种状态。处理期间禁用按钮；成功后用服务端返回余额更新页面。

- [ ] **步骤 4：实现单抽与十连按钮**

点击时创建新的请求编号。余额不足时不调用云函数；请求进行中禁用两个按钮；成功后打开结果层，失败后恢复按钮但保留同一请求编号用于明确的重试动作。

- [ ] **步骤 5：实现清新纪念册视觉**

使用明亮背景、拍立得式卡池封面、四档 CSS 变量和固定底部操作区。不得引入 3D 库或大型 UI 框架。

- [ ] **步骤 6：在微信开发者工具验证状态切换**

验证：首次进入、未签到、签到成功、余额不足、单抽等待、十连等待、接口失败、管理员入口显示与普通用户隐藏。

- [ ] **步骤 7：提交**

```powershell
git add miniprogram
git commit -m "feat: build sign in and draw home page"
```

---

### Task 9：实现抽卡动画、十连汇总与卡册页面

**文件：**

- 创建：`miniprogram/components/card-tile/index.js`
- 创建：`miniprogram/components/card-tile/index.wxml`
- 创建：`miniprogram/components/card-tile/index.wxss`
- 创建：`miniprogram/components/card-tile/index.json`
- 创建：`miniprogram/components/draw-result/index.js`
- 创建：`miniprogram/components/draw-result/index.wxml`
- 创建：`miniprogram/components/draw-result/index.wxss`
- 创建：`miniprogram/components/draw-result/index.json`
- 创建：`miniprogram/pages/album/index.js`
- 创建：`miniprogram/pages/album/index.wxml`
- 创建：`miniprogram/pages/album/index.wxss`
- 创建：`miniprogram/pages/album/index.json`
- 创建：`miniprogram/pages/card-detail/index.js`
- 创建：`miniprogram/pages/card-detail/index.wxml`
- 创建：`miniprogram/pages/card-detail/index.wxss`
- 创建：`miniprogram/pages/card-detail/index.json`

**接口：**

- `<draw-result results="{{results}}" />` 处理单抽和十连展示。
- `<card-tile card="{{card}}" />` 处理已获得和剪影两种状态。

- [ ] **步骤 1：实现卡牌组件固定尺寸和四档外观**

卡牌使用固定宽高比，加载、灰色剪影、已获得和图片失败状态不得改变布局尺寸。未获得卡必须使用灰度遮罩且不加载真实图片；卡框颜色由内部稀有度 ID 映射，显示名称来自服务端。

- [ ] **步骤 2：实现结果组件状态机**

状态为 `BACK -> REVEALED -> SUMMARY`。单抽从卡背翻至结果；十连逐张推进，用户可点击“跳过”直接进入汇总。退出结果层不得再次发起抽卡请求。

- [ ] **步骤 3：实现首次获得和重复数量反馈**

`isNew=true` 显示“新”；否则显示“已拥有 ×N”。最高稀有度增加短光效和可关闭音效，不自动长时间循环。

- [ ] **步骤 4：实现卡册列表与筛选**

页面加载调用 `listAlbum`。筛选项为全部、N、R、SR、SSR；页面顶部显示 `已收集 X/Y`。长列表分批渲染，每批不超过 40 项。

- [ ] **步骤 5：实现详情页**

只有 `owned=true` 的卡可进入详情。详情展示大图、名称、稀有度和拥有数量，不提供保存、分享、评论或交易按钮。

- [ ] **步骤 6：真机验证布局与失败状态**

至少使用两个不同屏幕尺寸验证按钮、卡片和文字无重叠；关闭网络验证占位图和错误提示。

- [ ] **步骤 7：提交**

```powershell
git add miniprogram/components miniprogram/pages/album miniprogram/pages/card-detail
git commit -m "feat: add draw reveal and private album"
```

---

### Task 10：实现管理员卡牌、图片检测与审核流程

**文件：**

- 创建：`cloudfunctions/api/src/services/admin-service.js`
- 创建：`cloudfunctions/api/src/services/security-client.js`
- 创建：`cloudfunctions/api/test/admin-service.test.js`
- 创建：`miniprogram/pages/admin/cards/*`
- 创建：`miniprogram/pages/admin/card-edit/*`
- 修改：`cloudfunctions/api/src/router.js`

**接口：**

- 所有 `admin*` 服务入口首先调用 `requireAdmin(openid)`。
- 卡牌状态：`DRAFT`、`CHECKING`、`PENDING_CONFIRMATION`、`PUBLISHABLE`、`PUBLISHED`、`OFFLINE`、`FORCE_REMOVED`。
- `securityClient.checkImage({ fileId, openid }): Promise<{ passed, traceId, reason }>`。

- [ ] **步骤 1：写管理员权限和状态迁移测试**

覆盖：普通用户所有管理调用返回 `FORBIDDEN`；草稿不能直接发布；图片检测通过后进入待人工确认；确认授权后进入可发布；更换图片后审核状态重置；只改名称或权重不重置图片检测状态。

- [ ] **步骤 2：写发布校验测试**

覆盖空名称、非法稀有度、非正整数权重、限定开始时间不早于结束时间、未通过检测、未确认授权六种失败情况。

- [ ] **步骤 3：运行测试确认失败**

运行：`npm test`。

- [ ] **步骤 4：实现内容安全适配器**

适配器从云存储下载文件，在云函数中调用微信图片内容安全接口，并把微信返回值归一化为 `passed/traceId/reason`。测试中注入假客户端，不调用真实接口。

- [ ] **步骤 5：实现管理员列表和编辑页**

管理员先用 `wx.chooseMedia` 选择图片，再上传至 `card-drafts/<年月>/<随机文件名>`。上传后调用管理 API 创建草稿。表单只包含名称、稀有度、常驻/限定、权重和限定时间。

- [ ] **步骤 6：实现检测与人工确认交互**

页面明确显示检测状态。只有检测通过后才能勾选“已取得肖像授权”，确认动作需要二次弹窗。发布按钮只在状态为 `PUBLISHABLE` 时启用。

- [ ] **步骤 7：运行自动测试和真实图片检测**

本地运行：`npm test`。

云端分别上传一张合规测试图和一张无法读取的文件，验证通过流程与失败原因均能显示。

- [ ] **步骤 8：提交**

```powershell
git add cloudfunctions/api miniprogram/pages/admin
git commit -m "feat: add admin card moderation workflow"
```

---

### Task 11：实现概率发布、限定卡、下架、强制移除和日志

**文件：**

- 修改：`cloudfunctions/api/src/services/admin-service.js`
- 修改：`cloudfunctions/api/test/admin-service.test.js`
- 创建：`miniprogram/pages/admin/config/*`
- 创建：`miniprogram/pages/admin/logs/*`
- 修改：`miniprogram/pages/admin/cards/*`

**接口：**

- `publishRarityConfig({ openid, rarities, pityLimit })`。
- `changeCardStatus({ openid, cardId, action, reason? })`。
- 每个管理写操作产生一条 `admin_logs` 记录。

- [ ] **步骤 1：写概率版本发布测试**

覆盖：四档合计不是 `10000` 时拒绝；缺少固定档位时拒绝；正概率档位没有可发布卡时拒绝；成功发布创建新版本且旧版本保持不可变。

- [ ] **步骤 2：写下架和强制移除测试**

覆盖：普通下架不改变已有收藏可见性；限定到期停止抽取但保留收藏；强制移除必须填写原因，并把关联收藏标记为不可见；三个动作均写入操作日志。

- [ ] **步骤 3：运行测试确认失败**

运行：`npm test`。

- [ ] **步骤 4：实现配置页**

固定渲染 N、R、SR、SSR 四行。每行可编辑显示名称和万分比；页面实时显示合计值，只有合计 `10000` 且保底为正整数时允许提交。

- [ ] **步骤 5：实现发布快照与管理日志**

每次成功发布生成新版本号和完整快照。抽卡流水只保存版本号，不读取或修改旧快照。日志保存操作类型、目标、修改前后摘要、原因和时间。

- [ ] **步骤 6：实现普通下架和强制移除交互**

普通下架使用一次确认；强制移除必须输入原因并二次确认。页面使用不同颜色和文案，避免误操作。

- [ ] **步骤 7：运行测试并验证限定边界**

运行：`npm test`。在测试环境将限定时间设置为当前时间前后各 2 分钟，验证开始时包含、结束时不包含。

- [ ] **步骤 8：提交**

```powershell
git add cloudfunctions/api miniprogram/pages/admin
git commit -m "feat: add pool publishing and card lifecycle controls"
```

---

### Task 12：安全加固、隐私说明、集成验收与体验版发布

**文件：**

- 修改：`cloudfunctions/api/src/router.js`
- 修改：`cloudfunctions/api/src/repositories/cloud-repository.js`
- 创建：`miniprogram/pages/privacy/index.js`
- 创建：`miniprogram/pages/privacy/index.wxml`
- 创建：`miniprogram/pages/privacy/index.wxss`
- 创建：`miniprogram/pages/privacy/index.json`
- 创建：`docs/manual-test-checklist.md`
- 创建：`docs/release-checklist.md`
- 修改：`README.md`

**接口：**

- 所有响应携带 `requestId` 方便排错。
- 日志只记录内部用户 ID 或掩码身份，不输出完整 `OpenID`。

- [ ] **步骤 1：为所有 API 增加输入白名单和频率限制**

路由只接受已注册动作。签到、抽卡和管理员写接口按 `openid + action` 设置短时间调用限制；被限制时返回 `RATE_LIMITED`，不得改变数据。

- [ ] **步骤 2：配置数据库和云存储权限**

在云控制台把八个业务集合的小程序端直接读写权限关闭，确认所有业务仍通过云函数正常运行。卡牌图片通过云函数生成临时 URL；未获得卡不返回文件标识。

- [ ] **步骤 3：完成隐私说明页**

说明仅处理微信身份标识、签到状态、抽卡余额、抽卡流水和个人收藏；不采集昵称、头像、手机号和位置；说明管理员上传的人物图片已经确认授权以及授权撤回处理方式。

- [ ] **步骤 4：写手工验收清单并逐项执行**

清单必须逐项记录通过/失败和证据，覆盖需求说明书第 13 章及以下高风险场景：北京时间跨日、重复签到、重复抽卡请求、十连回滚、保底边界、限定开始/结束、普通用户伪造管理请求、普通下架、强制移除和图片加载失败。

- [ ] **步骤 5：运行完整自动测试**

```powershell
Set-Location cardory/cloudfunctions/api
npm test
```

预期：退出码 0，无失败测试。

- [ ] **步骤 6：执行数据一致性抽查**

选择一个测试用户，人工核对：初始余额 + 签到次数 - 抽取消耗 = 当前余额；收藏数量等于抽卡流水中的卡牌出现次数；当前保底计数等于最后一次 SSR 之后的连续非 SSR 抽数。

- [ ] **步骤 7：在两种真机尺寸完成端到端测试**

每台设备完成：登录、签到、单抽、十连、查看卡册、查看详情。管理员设备额外完成：上传、检测、授权确认、发布、普通下架和强制移除。记录截图和发现的问题。

- [ ] **步骤 8：修复验收失败项并重新运行受影响测试**

每个修复先增加能复现问题的失败测试，再实现修复，最后运行 `npm test` 和对应手工场景。

- [ ] **步骤 9：准备体验版**

检查云环境不是开发者个人临时环境，检查 `admins` 白名单、有效概率配置、至少一个有效卡牌和隐私说明。使用微信开发者工具上传体验版，并把测试成员加入体验名单。

- [ ] **步骤 10：最终提交**

```powershell
git add .
git commit -m "release: prepare cardory experience build"
git status --short
```

预期：提交成功，最终 `git status --short` 无输出。

---

## 2. 任务与需求覆盖关系

| 需求区域 | 实施任务 |
| --- | --- |
| 静默登录、用户初始化、管理员识别 | Task 3 |
| 每日签到、次数累计、北京时间 | Task 2、Task 4 |
| 四档概率、权重、保底 | Task 2、Task 5、Task 6 |
| 单抽、十连、事务与幂等 | Task 5、Task 6 |
| 卡册、剪影、重复数量 | Task 7、Task 9 |
| 抽卡首页与结果动画 | Task 8、Task 9 |
| 图片上传、内容检测、肖像确认 | Task 10 |
| 限定时间、配置发布、版本快照 | Task 2、Task 11 |
| 普通下架、强制移除、操作日志 | Task 7、Task 11 |
| 权限、隐私、频率限制、验收 | Task 3、Task 10、Task 12 |
| 体验版交付 | Task 12 |

## 3. 每日目标映射

| 开发日 | 主要任务 | 当日可验证结果 |
| --- | --- | --- |
| 第 1 天 | Task 1 | 小程序和云函数骨架可运行，测试入口可用 |
| 第 2 天 | Task 2–3 | 时间、卡牌状态、登录和管理员识别可测 |
| 第 3 天 | Task 4 | 签到幂等和跨日规则通过 |
| 第 4–5 天 | Task 5–6 | 单抽、十连、权重、保底和事务通过 |
| 第 6 天 | Task 7 | 卡册数据规则通过 |
| 第 7 天 | Task 8 | 抽卡首页在真机可用 |
| 第 8 天 | Task 9 | 抽卡结果和卡册界面可用 |
| 第 9–10 天 | Task 10 | 管理员上传、检测和审核闭环可用 |
| 第 11–12 天 | Task 11 | 配置、限定卡、下架和日志可用 |
| 第 13–14 天 | Task 12 | 安全检查、完整验收和体验版发布 |

## 4. 实施完成判定

只有同时满足以下条件，才能宣告体验版实施完成：

1. `npm test` 最新一次运行退出码为 0。
2. `docs/manual-test-checklist.md` 中所有阻断级场景均有通过证据。
3. 一个普通测试用户完成登录、签到、单抽、十连、卡册和详情全流程。
4. 管理员完成上传、内容检测、授权确认、配置发布、上架、下架和强制移除全流程。
5. 两种不同屏幕尺寸的真机不存在按钮或文字重叠。
6. 数据一致性抽查符合余额、流水、收藏和保底关系。
7. 微信开发者工具已成功上传体验版，测试成员可以打开。
